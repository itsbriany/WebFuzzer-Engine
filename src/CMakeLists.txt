include_directories(${INCLUDE_DIRS})
link_directories(${LIBRARY_DIRS})
file(GLOB ENGINE_SRC "*.h" "*.cpp")
add_executable(WebFuzzerEngine ${ENGINE_SRC})
target_link_libraries(WebFuzzerEngine ${LIBRARIES} boost_log network-uri cppnetlib-client-connections cppnetlib-server-parsers)
